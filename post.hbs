{{!< default}}

{{#post}}

<main id="gh-main" class="gh-main">
    <article class="gh-article {{post_class}} {{#has tag="#hide-feature-image"}}no-image{{/has}}">

        <header class="gh-article-header gh-canvas">
            <span class="gh-article-meta">
                Posted <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="DD MMM YYYY"}}</time>
                {{#if primary_tag}}
                    in
                    <a class="gh-article-tag" href="{{primary_tag.url}}">{{primary_tag.name}}</a>
                {{/if}}
            </span>

            <h1 class="gh-article-title">{{title}}</h1>

            {{#if custom_excerpt}}
                <p class="gh-article-excerpt">{{custom_excerpt}}</p>
            {{/if}}

            {{^has tag="#hide-feature-image"}}
            {{> "feature-image"}}
            {{/has}}
        </header>

        <div class="gh-content gh-canvas">
            {{#has tag="license-cc-by-nc-sa-4-0"}}
            <div class="kg-card kg-callout-card kg-callout-card-green"><div class="kg-callout-emoji">©️</div><div class="kg-callout-text">Creative works shared in this post are, unless otherwise noted, licensed under the <a href="https://creativecommons.org/licenses/by-nc-sa/4.0/?ref=localhost" rel="noreferrer">Creative Commons Attribution-NonCommercial-ShareAlike 4.0 license</a>.</div></div>
            {{/has}}

            {{content}}
        </div>

        {{#if comments}}
            <div class="gh-comments gh-canvas">
                {{comments}}
            </div>
        {{/if}}

        <footer class="gh-article-footer gh-canvas{{#if @site.comments_enabled}}{{#unless @member}} no-border{{/unless}}{{#unless access}} no-border{{/unless}}{{/if}}">
            <nav class="gh-navigation">
                <div class="gh-navigation-previous">
                    {{#prev_post}}
                        <a class="gh-navigation-link" href="{{url}}">
                            <span class="gh-navigation-label">{{> icons/arrow-left}} Previous post</span>
                            <h4 class="gh-navigation-title">{{title}}</h4>
                        </a>
                    {{/prev_post}}
                </div>

                <div class="gh-navigation-middle"></div>

                <div class="gh-navigation-next">
                    {{#next_post}}
                        <a class="gh-navigation-link" href="{{url}}">
                            <span class="gh-navigation-label">Next post {{> icons/arrow-right}}</span>
                            <h4 class="gh-navigation-title">{{title}}</h4>
                        </a>
                    {{/next_post}}
                </div>
            </nav>
        </footer>

    </article>
</main>

{{/post}}
